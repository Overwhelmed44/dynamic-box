# dynamic-box

sing-box with remote user fetching

### Features

- Fetch users from remote (with caching)
- One IP per UUID at a time

### Environment variables

- CACHE_SIZE (default: 1000)
- FETCH_URL
